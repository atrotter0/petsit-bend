<div class="content">
  <h1 align="center">Pets</h1>
  <div class="data-content">
    <a href="<%= new_pet_path %>" class="btn btn-lg btn-primary" title="Add Pet">
      <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
      Add Pet
    </a>
  </div>
  <div class="data-container clearfix">
    <% if current_user.pets.present? || current_user.admin? %>
      <div class="data-header">
        <ul class="ul-list">
          <li class="li-list"><div class="data-icon"><img src="https://s3.amazonaws.com/petsit-bend/users.jpg" id="icon" class="img-circle" alt="User icon"></div></li>
          <li class="li-list"><div class="data-icon"><img src="https://s3.amazonaws.com/petsit-bend/pets.jpg" id="icon" class="img-circle" alt="Pet icon"></div></li>
          <li class="li-list"><div class="data-icon"><img src="https://s3.amazonaws.com/petsit-bend/options.jpg" id="icon" class="img-circle" alt="Options icon"></div></li>
        </ul>
      </div>
      <div class="data-content">
        <% if current_user.admin? %>
          <%= render 'admin/pets_index.html.erb' %>
        <% else %>
          <% @pets.each do |pet| %>
            <ul class="ul-list">
              <li class="li-list"><%= pet.user.first_name + ' ' + pet.user.last_name %></li>
              <li class="li-list"><%= pet.name %></li>
              <li class="li-list">
                <%= link_to 'View', pet_path(pet), class: "btn btn-med btn-primary" %>
                <%= link_to 'Edit', edit_pet_path(pet), class: "btn btn-med btn-primary" %>
                <%= link_to 'Delete', pet_path(pet), method: :delete,
                  data: { confirm: "Are you sure you want to delete this pet?" },
                    class: "btn btn-med btn-primary" %>
              </li>
            </ul>
          <% end %>
        <% end %>
      </div>
      <div class="paginate-box center">
          <%= will_paginate %>
      </div>
    <% end %>
  </div>
</div>
