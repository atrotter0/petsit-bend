<%= render 'shared/errors', obj: @lead %>

  <% provide(:title, "Petsit Bend | Contact") %>
  <div class="form-container small-form-width">
    <div class="row">
      <div class="col-xs-12">
        <%= form_for(@lead, :html => { class: 'form-horizontal', role: 'form' }) do |f| %>
          <div class="form-group">
              <%= f.label :first_name, class: 'label-style' %>
              <%= f.text_field :first_name, class: 'form-control', placeholder: 'First Name' %>
          </div>
          <div class="form-group">
            <%= f.label :last_name, class: 'label-style' %>
            <%= f.text_field :last_name, class: 'form-control', placeholder: 'Last Name' %>
          </div>
          <div class="form-group">
            <%= f.label :email, class: 'label-style' %>
            <%= f.email_field :email, class: 'form-control', placeholder: 'Email' %>
          </div>
          <div class="form-group">
            <%= f.label :phone, class: 'label-style' %>
            <%= f.text_field :phone, class: 'form-control', placeholder: 'Phone Number' %>
          </div>
          <div class="form-group">
            <%= f.label :message, class: 'label-style' %>
            <%= f.text_area :message, class: 'form-control', placeholder: "Type your message here.", rows: 10 %>
            <div id="contact-count">0 / 300 characters</div>
          </div>
          <div class="form-group">
            <%= f.submit "Submit", class: "btn btn-lg btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
